1. Сделать в нашем корпоративном кулькуляторе UI для вызовов и показа результатов вычислений (использующий WCF сервис бизнес-логики).

2. Добавить в репозиторий метод GetLogFiltered(<лямбда, описывающая фильтр>). Потестировать его в юнит тесте. Убедитесь, что фильтрация присходит на уровне СУБД.

3. На WCF сервисе добавить метод, возвращающий лог в дипазоне двух дат (с использованием ранее созданного метода у репозитория).

4. На WPF LogViewer'e добавить окошки для двух дат и кнопку, по которой присходит фильтрация (с использованием WCF пункта 3).

5. Добавьте что-то, чтобы LogViewer выглядел эффектнее.

При сдаче задачи: (1) поменяйте адреса endpoint'ов так, чтобы они были не localhost, а Ваш IP (2) WCF хост на своем компе оставьте запущенным.